<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sangeng.sangengspringsecurity.mapper.SysMenuMapper">

    <select id="getPermsByUserId" resultType="java.lang.String">
        select m.permissions p from sys_role_user ru left join sys_role r on ru.role_id = r.id
                                                    left join sys_role_menu rm on rm.role_id = ru.role_id
                                                    left join sys_menu m on m.id = rm.menu_id
		where ru.user_id = #{userId,jdbcType=BIGINT} and m.iz_show = 1
    </select>
</mapper>